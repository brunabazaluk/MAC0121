#include <stdlib.h>
#include <stdio.h>


int main()
{
	int * tam, n, i, j;
	char **c;

	for(i=0;i<n;i++)
		j=0;
		scanf("%c", &c[i][0]);
		while(c[i][j] != "\n")
			scanf("%c", &c[i][j]);
			j++;
		tam[i]=j;

	for (i=0; i<n;i++)
		printf("%s\n", c[i]);

}